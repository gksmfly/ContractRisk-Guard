# requirements-serve.txt
# 프로덕션 서빙(Dockerfile)용 의존성 부분집합.
#
# requirements-ci.txt와 동일한 이유로 requirements.txt 전체(크롤링/토픽모델링
# 스택)는 필요 없다 — 차이점은 pytest 등 테스트 도구 대신, 실제 요청 처리 시점에만
# 쓰이는 sentence-transformers(KoE5 임베더, backend/db/loader.py의 get_embedder())가
# 추가로 필요하다는 것 (유닛 테스트는 lifespan을 안 태우므로 이게 필요 없었음).
#
# requirements.txt에 API 쪽 의존성을 추가/변경하면 여기도 같이 갱신할 것.

fastapi
uvicorn[standard]
pydantic
python-multipart
pypdf>=4.0.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0
openai>=1.30.0
langgraph>=0.2.0
torch>=2.1.0
transformers>=4.36.0
sentencepiece>=0.1.99
sentence-transformers>=2.7.0
scikit-learn>=1.3.0
pandas>=2.1.0
