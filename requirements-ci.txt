# requirements-ci.txt
# CI(단위 테스트)용 최소 의존성 부분집합.
#
# requirements.txt 전체(playwright, bertopic, umap-learn, hdbscan, top2vec,
# contextualized-topic-models, konlpy 등 데이터 수집·토픽모델링 파이프라인용)를
# 그대로 설치하면 CI가 몇 분씩 걸리고 네이티브 빌드(Java/mecab 등)까지 필요해진다.
# `pytest -m "not integration"`이 실제로 import하는 건 API 서빙 경로
# (backend/api, backend/agents, backend/db)뿐이라 그 부분만 추린다.
#
# backend/agents/judgment_agent.py가 KoELECTRA 추론에 torch+transformers를
# 모듈 최상단에서 import하므로 이 둘은 무거워도 뺄 수 없다.
#
# requirements.txt에 API 쪽 의존성을 추가/변경하면 여기도 같이 갱신할 것.

fastapi
uvicorn[standard]
pydantic
python-multipart
pypdf>=4.0.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0
openai>=1.30.0
langgraph>=0.2.0
torch>=2.1.0
transformers>=4.36.0
sentencepiece>=0.1.99
scikit-learn>=1.3.0
pandas>=2.1.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.26.0
